# Superseded

This dual-runtime and copied-resource proposal is replaced by
`evolve-openhub-multi-runtime-control`, which covers Codex, Hermes, and OpenCode
and uses live access to the canonical knowledge root instead of copied mirrors.
