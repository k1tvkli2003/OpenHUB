# Built-in Codex CLI TLS fingerprint

OpenHUB uses the Python ssl module (OpenSSL) default TLS configuration for ChatGPT upstream calls. No browser impersonation or custom TLS fingerprint is applied. This aligns with the native Codex CLI TLS behavior.
