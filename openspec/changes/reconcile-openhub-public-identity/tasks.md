## 1. Contract and asset inventory

- [x] 1.1 Confirm Pages deploy ownership, public package availability, OAuth
      client ownership, approved logo hash, and every active stale identity.
- [x] 1.2 Define the truthful public identity, compatibility, and non-claim
      boundaries before implementation.
- [x] 1.3 Add and inspect the generated transparent OpenHUB wordmark without
      modifying the approved hex-lightning logo.

## 2. Product identity implementation

- [x] 2.1 Rebuild the Pages home/navigation/assets around verified OpenHUB
      capabilities and remove legacy screenshots from published docs.
- [x] 2.2 Rebrand browser/local OAuth completion surfaces and add the official
      provider-hosted identity disclosure to native and web account flows.
- [x] 2.3 Generate `sk-openhub-` keys, keep legacy keys valid, and update public
      examples without rewriting historical specifications.
- [x] 2.4 Rename the OpenHUB startup shortcut, remove unsupported registry/OCI
      claims, and add explicit upstream-project contributor thanks.

## 3. Verification and publication

- [x] 3.1 Add focused public-identity, API-key compatibility, OAuth template,
      installer, and documentation tests.
- [x] 3.2 Run strict OpenSpec/docs checks, backend/frontend/native tests and
      builds, secret/personal-path scans, and packaged runtime smoke checks.
- [x] 3.3 Inspect rendered Pages and packaged OpenHUB identity on both light and
      dark surfaces.
- [x] 3.3a Make release verification fail fast on every external command and
      keep native protocol tests valid for ephemeral prerelease versions.
- [ ] 3.4 Publish the green commit, wait for CI/Pages, create the stable Windows
      release, upload the portable archive with its install script, checksums,
      SBOM, and attestation, and verify
      every public artifact from GitHub.
