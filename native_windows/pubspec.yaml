name: openhub_windows
description: "Native Windows operator client for the local openhub runtime."
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  crypto: 3.0.7
  flutter:
    sdk: flutter
  path: 1.9.1
  sqlite3: 3.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/brand/openhub-route-hub.png
    - assets/brand/openhub-wordmark.png
