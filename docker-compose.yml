# NOTE: This Compose file defines a SINGLE-REPLICA topology.
# Do not use `docker compose up --scale server=N` — the SQLite default supports
# only one writer and the published host ports collide. Multi-replica deployments
# require the Helm chart with PostgreSQL and leader election (deploy/helm/openhub).
services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.local
    ports:
      - "2455:2455"
      - "1455:1455"
    command:
      # uvicorn directly (not `fastapi run`) so --ws-max-size matches the
      # websocket ingress budget that app.cli applies in production.
      - uvicorn
      - app.main:app
      - --no-proxy-headers
      - --host
      - 0.0.0.0
      - --port
      - "2455"
      - --reload
      - --ws-max-size
      - "134217728"
    volumes:
      - openhub-data:/var/lib/openhub
    develop:
      watch:
        - action: sync
          path: ./app
          target: /app/app
        - action: rebuild
          path: ./pyproject.toml
        - action: rebuild
          path: ./uv.lock
        - action: rebuild
          path: ./Dockerfile
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile_inline: |
        FROM oven/bun:1.3.14-alpine
        WORKDIR /app
        COPY package.json bun.lock ./
        RUN bun install --frozen-lockfile
        COPY . .
        CMD ["bun", "run", "dev", "--host", "0.0.0.0"]
    ports:
      - "5173:5173"
    environment:
      API_PROXY_TARGET: http://server:2455
    develop:
      watch:
        - action: sync
          path: ./frontend
          target: /app
          ignore:
            - node_modules/
        - action: rebuild
          path: ./frontend/package.json
        - action: rebuild
          path: ./frontend/bun.lock

  postgres:
    image: postgres:18-alpine
    profiles: ["postgres"]
    entrypoint:
      - sh
      - -ceu
      - |
        if [ -f /var/lib/postgresql/PG_VERSION ]; then
          echo >&2 "Detected a pre-Postgres-18 data directory at /var/lib/postgresql."
          echo >&2 "Run: docker compose --profile postgres-upgrade run --rm postgres-upgrade"
          echo >&2 "Refusing to start postgres:18-alpine before the named volume is upgraded."
          exit 1
        fi
        if [ -f /var/lib/postgresql/data/PG_VERSION ]; then
          pg_major="$$(cat /var/lib/postgresql/data/PG_VERSION)"
          if [ "$$pg_major" != "18" ]; then
            echo >&2 "Detected a nested Postgres $$pg_major data directory at /var/lib/postgresql/data."
            echo >&2 "Run pg_upgrade against that data directory before starting postgres:18-alpine."
            echo >&2 "For the default Compose volume layout, run: docker compose --profile postgres-upgrade run --rm postgres-upgrade"
            exit 1
          fi
        fi
        if [ "$#" -eq 0 ]; then
          set -- postgres
        fi
        exec docker-entrypoint.sh "$@"
      - openhub-postgres-entrypoint-guard
    environment:
      POSTGRES_USER: openhub
      POSTGRES_PASSWORD: openhub
      POSTGRES_DB: openhub
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U openhub -d openhub"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - openhub-postgres-data:/var/lib/postgresql
    restart: unless-stopped

  postgres-upgrade:
    image: pgautoupgrade/pgautoupgrade:18-alpine@sha256:60580d98bb0c948efcd628d322091e965c1493ea6ccc4325ce22fc4dbd4dfa38
    profiles: ["postgres-upgrade"]
    environment:
      POSTGRES_USER: openhub
      POSTGRES_PASSWORD: openhub
      POSTGRES_DB: openhub
      PGAUTO_ONESHOT: "yes"
    volumes:
      - openhub-postgres-data:/var/lib/postgresql
    restart: "no"

networks:
  default:
    driver: bridge

volumes:
  openhub-data:
    name: openhub-data
  openhub-postgres-data:
    name: openhub-postgres-data
