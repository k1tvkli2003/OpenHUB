apiVersion: v1
kind: Service
metadata:
  name: {{ include "openhub.bridgeHeadlessServiceName" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "openhub.labels" . | nindent 4 }}
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    {{- include "openhub.workloadSelectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
